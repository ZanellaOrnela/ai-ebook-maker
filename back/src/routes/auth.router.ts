import { Router } from "express";


const router = Router();

router.post("/register", );
router.post("/login", )
router.get("/google", )
router.get("/google/callback", )
router.post("/logout", )

export default router;