import { Router } from "express";


const router = Router();

router.post("/ebooks", );
router.get("/ebooks", )
router.get("/ebooks/:id", )
router.put("/ebooks/:id", )
router.delete("/ebooks/:id", )
router.post("/ebooks/:id/duplicate", )

export default router;